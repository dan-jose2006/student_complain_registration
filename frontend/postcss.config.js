// Export PostCSS configuration object
export default {
  // PostCSS plugin pipeline
  plugins: {
    // Tailwind CSS processor for utility classes
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes to CSS rules for broad browser compatibility
    autoprefixer: {},
  },
};

